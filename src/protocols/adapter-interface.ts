/**
 * Re-export the adapter interface and types from the shared types module.
 * This file exists to match the planned project structure.
 */
export {
  StreamAdapter,
  type StreamAdapterEvents,
  type AdapterOptions,
  type ConnectionState,
} from "../types.js";
